name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Install Oxidoc
        run: curl -fsSL https://raw.githubusercontent.com/oxidoc-lab/oxidoc/main/scripts/install.sh | sh

      - name: Build site
        run: oxidoc build

      - name: Add install scripts
        run: |
          curl -fsSL https://raw.githubusercontent.com/oxidoc-lab/oxidoc/main/scripts/install.sh -o dist/install.sh
          curl -fsSL https://raw.githubusercontent.com/oxidoc-lab/oxidoc/main/scripts/install.ps1 -o dist/install.ps1

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: dist

  deploy:
    name: Deploy to GitHub Pages
    needs: build
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
